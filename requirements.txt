# Core dependency — the only required package.
# 2.31.0 fixed CVE-2023-32681 (proxy credential leak).
# Upper bound <3 guards against breaking API changes.
requests>=2.31.0,<3

# Password hashing — install at least ONE of the following.
# scrypt is built into Python's hashlib (no install needed).
# bcrypt and argon2-cffi are optional stronger alternatives.
#
# Uncomment to install:
# bcrypt>=4.0.0,<5
# argon2-cffi>=23.1.0,<24
